#ifndef SUFFIX_PREFIX_LCS_LONG_H
#define SUFFIX_PREFIX_LCS_LONG_H

#include <vector>
#include <string>
#include <cstddef>

using std::size_t;

std::vector< std::vector<size_t> > calculateSuffixPrefixLCS_long(const std::string &s, const std::string &t);

std::vector< std::vector<size_t> > calculateSuffixPrefixLCS_long2(const std::string &s, const std::string &t);

#endif //SUFFIX_PREFIX_LCS_LONG_H
